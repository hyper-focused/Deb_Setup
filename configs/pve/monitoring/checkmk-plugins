# check_mk agent-local plugins — Proxmox VE (bare metal)
# Lines starting with # are skipped by init.sh
# Each active line is a filename from /opt/librenms-agent/agent-local/
# Exception: 'temperature' is served from configs/pve/monitoring/temperature (curated)

dpkg        # Debian package state
dmi         # Hardware DMI info
proxmox     # PVE VMs/containers — status, CPU, memory, disk via PVE API
rrdcached   # RRDcached stats (PVE uses for all graph storage)
temperature # CPU/system temps via lm-sensors (curated — upstream is a template)
