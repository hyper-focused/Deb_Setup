# /etc/snmp/smart.config — SMART monitoring template (PVE bare metal)
# The smart extend script reads this file to know which drives to check.
# Auto-populated by init.sh — edit to add/remove drives.
# Format: <label> <device> <driver-options>

useSN=0
smartctl=/usr/sbin/smartctl
cache=/var/cache/smart

# SATA/SAS drives — add one line per drive
# sda /dev/sda -d sat
# sdb /dev/sdb -d sat

# NVMe drives
# nvme0 /dev/nvme0 -d nvme
# nvme1 /dev/nvme1 -d nvme

# RAID controller (e.g., MegaRAID) — uncomment and adjust slot numbers
# sda /dev/sda -d megaraid,0
