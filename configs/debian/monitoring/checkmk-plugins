# check_mk agent-local plugins — Debian 13 QEMU VM
# Intentionally empty: VMs use SNMP extends only.
# dpkg/dmi check_mk plugins are replaced by SNMP (osupdate, DMI extends, distro).
# Re-enable check_mk on VMs only if you have a hard requirement for agent-local apps.
