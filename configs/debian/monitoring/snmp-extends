# SNMP extend scripts — Debian 13 QEMU VM
# One script name per line (from librenms-agent snmp/). Lines starting with # skipped.
# entropy.sh is installed as /etc/snmp/entropy (suffix stripped).
# Guest-only: no SMART / ZFS / postfix / hardware sensors.

linux_softnet_stat
chrony
osupdate
entropy.sh
